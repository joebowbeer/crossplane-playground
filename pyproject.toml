[project]
name = "crossplane-playground"
version = "0.1.0"
description = "Crossplane playground"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crossplane-function-pythonic>=0.0.7.post3",
]
